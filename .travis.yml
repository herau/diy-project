language: java

jdk:
  - oraclejdk8

env:
  - NODE_VERSION=0.12

before_install:
  - nvm install $NODE_VERSION
  - nvm use $NODE_VERSION
  - npm install
  - npm run build

before_script:
  - pip install --user codecov

after_success:
  - codecov

branches:
  only:
  - master
