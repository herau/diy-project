spring:
  main:
    show-banner: false
  jackson:
    deserialization:
      FAIL_ON_UNKNOWN_PROPERTIES: true

security:
  enable-csrf: false

app:
  email:
    subject: Subject
    registration:
      tokenExpiryTime: 0
